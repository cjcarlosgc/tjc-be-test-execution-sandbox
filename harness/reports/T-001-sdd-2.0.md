# T-001 — Reporte final SDD 2.0

Repository: `tjc-be-test-execution-sandbox`  
Branch: `feature/T-001`  
Files changed: mirrors, backlog/migration map, constituciones, feature 009, referencias contractuales legacy, state, changelog y reporte. `app/` no cambia.

SDD version: `2.0`  
SYSTEM version: `2.0`  
INTEROP version: `2.0`

Decisions approved: adopta las decisiones compartidas aprobadas de SYSTEM-2.0.  
Decisions modified: incorpora `ExecutionProfile`/`TestRunner` y la fase baseline/proposal.  
Decisions remaining pending: `DEC-MET-001`, `DEC-INF-001`, `DEC-VAL-001`, `DEC-EXP-FK-001`.

Backlog items added: HU30-HU47; participación directa HU43 y HU47.  
Legacy items marked superseded: ningún componente interno se elimina; el supuesto TypeScript-only pasa a profile de compatibilidad.

Architecture changes: pipeline neutral seleccionado por execution profile.  
Contract changes: `NODE_TYPESCRIPT`, `PHP_LARAVEL_PHPUNIT`, runner y phase.  
State-machine changes: Sandbox reporta fase/estado técnico; Core conserva la clasificación del Run.  
Security changes: mantiene ceguera a GitHub/usuarios/RAG/conocimiento y límites/red/cleanup.  
Stack-support changes: Node/pnpm/Jest/Vitest se preserva; PHP/Composer/PHPUnit se planifica como implementación real.

Implementation intentionally deferred: adapter e imagen PHP HU43 y VM remota HU47.  
Potential inconsistencies detected: specs antiguas con referencias INTEROP previas y TypeScript-only; se alinearon a 2.0 y profiles.

## Backend reorientation assessment

Existing completed capabilities to preserve: API asíncrona, Bearer/idempotencia, workspaces, containers, Jest/Vitest, evidencia y cleanup.  
Existing partial capabilities to adapt: transporte/DTO a profile y phase.  
Pending work to defer: proveedor remoto hasta `DEC-INF-001`.  
Pending work to drop: fake PHP runner.  
New PHP-related P0/P1 work: HU43.  
INTEROP-2.0 migration requirements: profile/runner/fase, Composer metadata y evidencia PHPUnit neutral.  
Recommended next Sandbox work item: abstracción real de execution profiles, seguida del runtime PHP; requiere selección humana.

```text
TypeScript/Jest/Vitest -> compatibility maintained
PHP/Laravel/PHPUnit -> active new-development focus
```

La migración GitHub y el human-in-the-loop terminan en Core; Sandbox solo recibe la ejecución neutral. Mirrors son byte-identical, state declara SDD 2.0, `git diff --check` pasa y `app/` está intacto. No se inicia implementación posterior.
