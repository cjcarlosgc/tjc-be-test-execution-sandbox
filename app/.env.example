PORT=3000

# Bearer token required from RAG Core on every request (INTEROP-1.1 §3).
SANDBOX_SERVICE_TOKEN=

# Milliseconds suggested to Core before polling execution status again.
SANDBOX_POLL_AFTER_MS=2000
